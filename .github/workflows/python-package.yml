# This workflow will install Python dependencies, run tests and lint with a variety of Python versions
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Python package

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule: 
    - cron: '30 1 * * *'

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.10", "3.11"]

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        
    - name: Install library
      run: |
        pip install requests
        pip install beautifulsoup4
        pip install lxml
        pip install pandas
        pip install selenium
        pip install webdriver_manager
      
    - name: run file
      run: |
        python3 pyBillboardChart.py
        python3 pyBugsChart.py
        python3 pyMelonChart.py
        python3 pyAppleChart.py

    - name: commits
      run : |
        git config --global user.email "hyeji1364@gmail.com"
        git config --global user.name "Hyeji1364" 
        git add concert/melon/chart_M_concert10_*.json
        git add concert/interpark/chart_I_concert10_*.json
        git add concert/ticketlink/chart_T_concert10_*.json
        git add concert/yes24/chart_Y_concert10_*.json

        git add exhibition/melon/chart_M_concert10_*.json
        git add exhibition/interpark/chart_I_concert10_*.json
        git add exhibition/ticketlink/chart_T_concert10_*.json
        git add exhibition/yes24/chart_Y_concert10_*.json

        git add musical/melon/chart_M_concert10_*.json
        git add musical/interpark/chart_I_concert10_*.json
        git add musical/ticketlink/chart_T_concert10_*.json
        git add musical/yes24/chart_Y_concert10_*.json
        git commit -m "차트 수집 완료"

    - name: push
      uses: ad-m/github-push-action@master
      with:
        branch: "main"
        github_token: $
      
